<% provide(:title, 'All users') %>
<h1>All users</h1>

<div class="mt-5">
  <%== pagy_bootstrap_nav(@pagy) %>
</div>
<ul class="list-unstyled">
  <%= render @users %>
</ul>
<div class="mt-5">
  <%== pagy_bootstrap_nav(@pagy) %>
</div>
